# Leave blank for same-origin API calls (recommended with Option 1 routing).
VITE_API_BASE_URL=
# Example for direct GitHub Pages testing:
# VITE_API_BASE_URL=https://trash.flaviof.com

# Optional base path for static asset URLs (defaults to `/`).
# For GitHub project pages this is usually `/<repo-name>/`.
VITE_BASE_PATH=

# Dev-only: if set, Vite proxies API routes to this backend target.
# Example: http://localhost:5000
VITE_DEV_PROXY_TARGET=

# Example Nginx http-context settings for rate limiting and header hardening.
# Place these inside the top-level `http { ... }` block (or in /etc/nginx/conf.d).

server_tokens off;
limit_req_zone $binary_remote_addr zone=ics_rate:10m rate=30r/m;
limit_conn_zone $binary_remote_addr zone=addr:10m;
